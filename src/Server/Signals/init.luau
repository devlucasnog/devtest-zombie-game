local ProfileSignals = require(script.ProfileSignals)
local MatchSignals = require(script.MatchSignals)
local CombatSignals = require(script.CombatSignals)

return {
	ProfileSignals = ProfileSignals,
	MatchSignals = MatchSignals,
	CombatSignals = CombatSignals,
}
