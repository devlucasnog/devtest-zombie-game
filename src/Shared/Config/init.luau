local WeaponConfig = require(script.WeaponConfig)
local MatchConfig = require(script.MatchConfig)
local ZombieConfig = require(script.ZombieConfig)

return {
	WeaponConfig = WeaponConfig,
	MatchConfig = MatchConfig,
	ZombieConfig = ZombieConfig,
}
