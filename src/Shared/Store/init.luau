local ProfileAtom = require(script.Atoms.ProfileAtom)
local AmmoAtom = require(script.Atoms.AmmoAtom)
local MatchAtom = require(script.Atoms.MatchAtom)
local WeaponAtom = require(script.Atoms.WeaponAtom)

return {
	Profile = ProfileAtom,
	Ammo = AmmoAtom,
	Match = MatchAtom,
	Weapon = WeaponAtom,
}
